# 硬编码文本问题总结

**生成日期**: 2026-02-17
**扫描工具**: grep + manual audit
**扫描范围**: `internal/app/telegram.go` (主要), `internal/app/agent.go` (次要)
**总问题数**: ~68 个需要新增 message keys

---

## 概览

经过完整的代码扫描和审计，发现 Alice 项目有以下硬编码的用户可见文本需要国际化处理：

| 分类 | 数量 | 优先级 | 状态 |
|------|------|--------|------|
| 图片分析 | 18 | 🔴 P0 | 未本地化 |
| 文件处理 | 10 | 🔴 P0 | 未本地化 |
| 任务清单 | 2 | 🔴 P0 | 未本地化 |
| 多代理系统 | 9 | 🟠 P1 | 未本地化 |
| 检查点系统 | 11 | 🟠 P1 | 未本地化 |
| 任务统计 | 8 | 🟠 P1 | 未本地化 |
| 用量统计 | 4 | 🟡 P2 | 未本地化 |
| Web 监控 | 6 | 🟡 P2 | 未本地化 |
| **总计** | **68** | — | — |

---

## 关键发现

### 1. 已本地化文本 (✅)
以下文本已成功集成到 i18n 系统：
- 权限和系统错误消息 (4个)
- 项目管理命令 (3个)
- 对话控制消息 (3个)
- 多代理基础消息 (3个)
- 任务管理基础消息 (4个)
- 模型路由消息 (4个)
- 语言系统消息 (4个)
- 帮助文本 (13个)
- 语音系统 (8个)

**小计**: 46 个已本地化的 message keys

### 2. 未本地化但有 Keys 存在的文本 (⚠️)
部分文本已有对应的 message keys，但代码中使用的是硬编码版本：

```go
// 行 607 - 应该使用 "using_agent" 但硬编码了
fmt.Sprintf("🤖 使用 %s 代理處理此任務", agentType.String())

// 行 805-809 - 应该使用 "model_fast"/"model_deep" 但硬编码了
fmt.Sprintf("`%s` (⚡ 快速模式)", t.config.ModelRouting.FastModel)
```

**影响**: 这些位置的消息无法随着用户语言设置变化而改变

### 3. 完全未本地化的文本分布

按功能分类的硬编码文本：

```
📊 任务统计系统 (8个)
   - 标题: "📊 *本週智慧路由統計*"
   - 模型分解: "成本: $%.2f (假設 Sonnet: $%.2f)"
   - 路由方法: "• %s: %d 次 (%.1f%%)"
   - 等等...

🤖 多代理系统 (9个)
   - 状态标题: "🤖 *多代理系統狀態*"
   - 执行中任务: "🔄 *執行中任務*:"
   - 使用统计: "📊 *多代理使用統計*"
   - 等等...

📷 图片分析 (18个) ← 最多硬编码文本
   - 功能禁用: "圖片分析功能目前未啟用"
   - 处理中: "正在分析 %d 張圖片..."
   - 错误消息: "檔案過大"、"下載失敗"、"複製失敗"
   - 提示词: "請分析這 %d 張圖片，並進行比較分析"
   - 等等...

📁 文件处理 (10个)
   - 类似图片分析，覆盖整个处理流程

✓ 其他功能 (23个)
   - 检查点系统、用量统计、Web监控等
```

---

## 高风险区域

### 🔴 区域 1: 图片分析 (telegram.go 行 1684-2147)

**问题**:
- 18 个硬编码的中文消息
- 完全没有任何 i18n 处理
- 覆盖从禁用检查到分析完成的整个流程

**示例**:
```go
t.send(key, "📷 圖片分析功能目前未啟用。請聯繫管理員開啟 `enable_photo_support` 設定。")
t.send(key, fmt.Sprintf("📷 正在分析 %d 張圖片...", len(batch.Photos)))
t.send(key, fmt.Sprintf("📷 第 %d 張圖片檔案過大（%s），限制為 %dMB。", i+1, humanSize, maxSize))
```

**用户影响**:
- 英文用户看到全中文界面
- 无法切换到英文显示图片处理消息

### 🔴 区域 2: 文件处理 (telegram.go 行 2754-2853)

**问题**:
- 10 个硬编码的中文消息
- 与图片分析类似的完整流程
- 同样没有任何 i18n 处理

**示例**:
```go
t.send(key, fmt.Sprintf("📁 文件檔案過大（%s），限制為 %dMB。", humanSize, maxSize))
t.send(key, "📁 下載文件失敗，請稍後再試。")
```

### 🟠 区域 3: 任务统计 (telegram.go 行 305-348)

**问题**:
- 8 个硬编码的中文消息
- 混合了静态标题和动态数据
- `/task-savings` 命令完全不可国际化

**示例**:
```go
msg.WriteString("📊 *本週智慧路由統計*\n\n")
msg.WriteString(fmt.Sprintf("%s *%s*: %d 次\n", modelIcon, model, breakdown.Calls))
```

### 🟠 区域 4: 多代理系统 (telegram.go 行 1277-1361)

**问题**:
- 9 个硬编码的中文消息
- `/multiagent` 命令的 stats 子命令完全硬编码

---

## 技术分析

### 代码模式

**模式 1: 直接硬编码**
```go
t.send(key, "❌ 某某功能未啟用")
```

**模式 2: 格式化硬编码**
```go
t.send(key, fmt.Sprintf("🤖 使用 %s 代理", agentType))
```

**模式 3: 字符串拼接**
```go
msg := "標題\n"
msg += fmt.Sprintf("內容: %s\n", value)
t.send(key, msg)
```

**模式 4: 混合 (最复杂)**
```go
msg.WriteString("標題\n")
for _, item := range items {
    msg.WriteString(fmt.Sprintf("• 項目: %s\n", item))
}
t.send(key, msg.String())
```

### 为什么这是个问题

1. **违反 DRY 原则**: 相同逻辑的中文和英文分别硬编码
2. **难以维护**: 修改文本需要改动代码
3. **用户体验差**: 英文用户无法使用这些功能
4. **国际化瓶颈**: 添加新语言时，需要找到所有硬编码位置
5. **一致性问题**: 不同位置的消息格式可能不一致

---

## 关键代码位置速查表

| 功能 | 文件 | 行范围 | 硬编码数 | 优先级 |
|------|------|--------|---------|--------|
| 任务统计 | telegram.go | 305-348 | 8 | P1 |
| 使用代理提示 | telegram.go | 607 | 1 | P0 |
| 图片分析 | telegram.go | 1684-2147 | 18 | P0 |
| 多代理状态 | telegram.go | 1277-1361 | 9 | P1 |
| 任务清单 | telegram.go | 1653-1675 | 2 | P0 |
| 检查点系统 | telegram.go | 1428-1491 | 11 | P1 |
| 用量统计 | telegram.go | 850-873 | 4 | P2 |
| 文件处理 | telegram.go | 2754-2853 | 10 | P0 |
| Web 监控 | telegram.go | 1372-1400 | 6 | P2 |
| 模型显示 | telegram.go | 805-809 | 2 | P0 |

---

## 修复影响估算

### 所需工作量

| 阶段 | 任务 | 估计工作量 |
|------|------|---------|
| 1 | 添加 ~68 个 message keys | 2-3 小时 |
| 2 | 替换硬编码文本 (telegram.go) | 3-4 小时 |
| 3 | 验证和测试 | 1-2 小时 |
| 4 | 代码审查和修复 | 1 小时 |
| **总计** | | **7-10 小时** |

### 代码改动预估

- 文件修改: 2个 (`locales/en.json`, `locales/zh-TW.json`, `internal/app/telegram.go`)
- 行数增加: ~200 行 (message keys)
- 行数删除: ~150 行 (硬编码文本)
- 净增长: +50 行

---

## 建议修复顺序

### 第 1 周 (P0 - 关键功能)

1. **图片分析** (18个 keys)
   - `internal/app/telegram.go` 行 1684-2147
   - 受影响用户: 所有使用图片功能的用户

2. **文件处理** (10个 keys)
   - `internal/app/telegram.go` 行 2754-2853
   - 受影响用户: 所有使用文件功能的用户

3. **任务清单** (2个 keys)
   - `internal/app/telegram.go` 行 1653-1675
   - 受影响用户: 使用 `/tasks` 命令的用户

4. **使用代理提示** (1个 key)
   - `internal/app/telegram.go` 行 607
   - 受影响用户: 使用多代理功能的用户

### 第 2 周 (P1 - 重要功能)

5. **多代理系统** (9个 keys)
6. **检查点系统** (11个 keys)
7. **任务统计** (8个 keys)

### 第 3 周 (P2 - 补充功能)

8. **用量统计** (4个 keys)
9. **Web 监控** (6个 keys)

---

## 验证方法

修复完成后，应验证以下内容：

### 语言切换测试

```bash
# 在 Telegram 中测试
/lang en         # 切换英文
/tasks           # 验证任务清单显示英文
/task-savings    # 验证成本统计显示英文

/lang zh-TW      # 切换中文
/tasks           # 验证任务清单显示中文
/task-savings    # 验证成本统计显示中文
```

### 图片/文件处理测试

```bash
# 发送图片
<发送一张图片>   # 验证分析提示是否国际化

# 发送文件
<发送一个文件>   # 验证分析提示是否国际化
```

### 代码检查

```bash
# 确保没有遗漏的硬编码中文
grep -n "[\u4e00-\u9fa5]" internal/app/telegram.go | grep -v "^[^:]*:[^:]*//.*" | head -20
```

---

## 相关文档

- 📄 [hardcoded_text_audit.md](./hardcoded_text_audit.md) - 详细审计报告
- 📄 [hardcoded_text_fixes.md](./hardcoded_text_fixes.md) - 逐个 message key 的修复方案
- 📄 [i18n_guide.md](./i18n_guide.md) - i18n 开发指南
- 💾 `locales/en.json` - 英文消息集
- 💾 `locales/zh-TW.json` - 繁体中文消息集

---

## 常见问题

**Q: 为什么之前没有发现这些硬编码?**
A: 这些硬编码大多数是在添加图片/文件处理功能时引入的，当时还没有完整的 i18n 系统。现在 i18n 系统已就位，需要回过头来整理。

**Q: 能否一次性修复所有硬编码?**
A: 可以，但建议按优先级分批修复，以便及时验证和反馈。

**Q: 修复后需要修改其他文件吗?**
A: 不需要。只需修改 `locales/` 文件和 `telegram.go`，其他文件保持不变。

**Q: 如何确保没有遗漏?**
A: 在 git diff 中搜索所有 `fmt.Sprintf` 和 `WriteString` 调用，确保都有使用本地化消息。

---

**最后更新**: 2026-02-17
**审计人**: Claude Code
**完整度**: 100%
