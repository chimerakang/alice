apiVersion: v1
kind: Secret
metadata:
  name: alice-secrets
  labels:
    app: alice-bot
type: Opaque
stringData:
  telegram-bot-token: "CHANGE_ME"
  encryption-key: "CHANGE_ME_32_CHARS_LONG_KEY_12345"
  # Add other sensitive configuration here
  # Note: In production, use external secret management (e.g., Vault, AWS Secrets Manager)