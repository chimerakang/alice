apiVersion: v1
kind: ConfigMap
metadata:
  name: alice-config
  labels:
    app: alice-bot
data:
  claude-model: "sonnet"
  rate-limit-rpm: "60"
  data-retention-days: "30"
  web-port: "8080"
  log-level: "info"
  # Add other non-sensitive configuration here